program Exercicio;

var A, B, C, D, E, F, X: real;

begin
  read(A);
  read(B);
  read(C);
  read(D);
  read(E);
  read(F);
  x := (((A + B) / (C - D)) * E) / ((F / (A * B)) + E);
  writeln(X);
end.

//Correto.