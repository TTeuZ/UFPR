#include <stdio.h>
#include <stdlib.h>
#include "libconjunto.h"

conjunto_t * cria_conjunto(int max) {
    conjunto_t conj;
    // if (max > MAX) return 0;
    // else {
    //     if (*conj.v = malloc(max)) {
    //         conj.card = max;
    //         return *conj.v;
    //     } else return 0;
    // }
}