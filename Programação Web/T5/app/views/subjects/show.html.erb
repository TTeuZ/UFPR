<div class="mx-3 my-3">
  <div class="w-100 border-bottom mb-4 d-flex flex-row justify-content-between align-items-center">
    <h2> Sobre <%= @subject.name %> </h2>

      <% if current_user.user_type == 'admin' %>
        <div class="d-flex flex-row align-items-center">
          <button type="button" class="btn btn-outline-primary">
            <%= link_to "Editar", edit_subject_path(@subject) %>
          </button>
          <%= button_to "Apagar", @subject, method: :delete, class: 'btn btn-outline-danger text-white ms-3' %>
          <button type="button" class="btn btn-outline-light ms-3">
            <%= link_to "Voltar", subjects_path %>
          </button>
        </div>
      <% end %>
  </div>
  <div class="d-flex flex-column justify-content-between">
    <span> <strong>Id:</strong> <%= @subject.id %> </span>
    <span> <strong>Nome:</strong> <%= @subject.name %> </span>
    <span> <strong>Código:</strong> <%= @subject.code %> </span>
  </div>
</div>